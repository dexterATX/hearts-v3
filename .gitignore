# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
expo-env.d.ts

# native projects are generated by CNG prebuild — never commit them
android/
ios/

# env & secrets — .env.example is the template, .env stays local
.env
.env.local
.env.*.local

# Firebase android config — carries the FCM api key; keep it local and
# upload it to EAS as a file secret for cloud builds
google-services.json

# supabase local
supabase/.branches/
supabase/.temp/
supabase/.env

# builds
*.apk
*.aab
*.ipa

# os/editor
.DS_Store
.idea/
.vscode/
*.log

# release signing — the keystore and its passwords never go in git
credentials/
.claude/
credentials.json
